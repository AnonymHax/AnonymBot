#include "MariedItemCriterionData.h"

void MariedItemCriterionData::loadData(const QList<Field*> &fields, I18nFile *I18n)
{
  ItemCriterionData::loadData(fields, I18n);
  
  foreach (Field *field, fields)
  {
  }
}

